const express = require('express');
const app = express();
const port = 444;
app.get('/', (req, res) => {
    res.send('Hello Node JS!');
});
app.listen(port, () => {
    console.log(`Running on : ${port}`); 
})
